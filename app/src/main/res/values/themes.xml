<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Schach1337" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Schach1337" parent="Base.Theme.Schach1337" />

    <style name="GameOverMenu" parent="Theme.AppCompat.Light.Dialog">
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowIsFloating">true</item>
        <item name="windowMinWidthMinor">90%</item>
        <item name="windowMinWidthMajor">90%</item>
        <item name="colorPrimary">@color/p_color</item>
        <item name="colorPrimaryDark">@color/p_d_color</item>
    </style>

</resources>